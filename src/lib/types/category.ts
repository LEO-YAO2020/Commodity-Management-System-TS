export interface CategoryUpdateInter {
  categoryId: Number;
  categoryName: string;
}

export interface CategoryAddInter {
  categoryName: string;
}

export interface CategoryItems {
  _id: string;
  name: string;
}
